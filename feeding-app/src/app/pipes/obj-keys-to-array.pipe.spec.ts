import { ObjKeysToArrayPipe } from './obj-keys-to-array.pipe';

describe('ObjKeysToArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new ObjKeysToArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
